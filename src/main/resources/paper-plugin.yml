name: Syncmoney
version: '${version}'
main: noietime.syncmoney.Syncmoney
description: Cross-server game currency sync via Vault (Redis + DB).
api-version: '1.20'
folia-supported: true

dependencies:
  server:
    Vault:
      load: AFTER
      required: true
softdepend:
  - CMI
  - PlaceholderAPI

